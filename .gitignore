# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — exchange API keys live here and must never be committed.
# A leaked key with trading permission is drained within minutes.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local database files. The Mongo data directory and the old SQLite file are
# machine-local state, not source — and they hold real user records.
server/data/
backend/data/

# Python bytecode from the freqtrade research scaffold.
__pycache__/
*.pyc
